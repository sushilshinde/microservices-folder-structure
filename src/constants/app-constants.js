const config = require('config')

/**
 * App constants
 */
const UserRoles = {
    Admin: 'Administrator',
    Manager: 'Manager',
    User: 'Normal User'
}

const SOME_CONSTANT = '123'

module.exports = {
    UserRoles,
    SOME_CONSTANT
}