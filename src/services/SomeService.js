const config = require('config')

/**
 * Get Data
 * @param {Number} id some id
 */
 async function getServiceData (id) {
    
    //some IO/NW calls
    
    return [1,2,3]
  }

  module.exports = {
    getServiceData
  }